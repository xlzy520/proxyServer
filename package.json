{
  "name": "express-starter",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "nodemon ./bin/www",
    "preview": "webpack --progress && cross-env mode=preview npm run start",
    "deploy": "npm run build && sls deploy",
    "analyzer": "cross-env use_analyzer=true npm run build"
  },
  "author": "xlzy520",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.26.1",
    "cross-env": "^7.0.3",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
